<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adimer.natividad">

    <application
        android:allowBackup="true"
        android:icon="@drawable/natividad"
        android:label="@string/app_name"
        android:roundIcon="@drawable/natividad"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".p4"></activity>
        <activity android:name=".p3" />
        <activity android:name=".p2" />
        <activity android:name=".ultimo" />
        <activity android:name=".tienes" />
        <activity android:name=".como" />
        <activity android:name=".Recomendacion" />
        <activity android:name=".covid" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>